$(document).ready(function(){

	$(".frontButton").on("click", function() {
		$(".rain, .pulse, .sun, .frontButton").hide();
		window.location.href = '/home/new';
	});	

	// Sellection of background image based on icon being loaded to visually match the weather pattern
	
	$(".img_cloudy").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('cloud-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	});

	$(".img_sunny").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('sun-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	});

	$(".img_rain").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('rain-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	});

	$(".img_thunderstorm").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('thunderstorm-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	});

	$(".img_snow").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('snow-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	}); 

	$(".img_fog").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('fog-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	});

	$(".img_sleet").load(function() {
		$("#newPage").css("background-image","url('<%= image_path('sleet-bgr.jpg') %>')");
		}).each(function() {
	  		if(this.complete) $(this).load();
	});    	
});